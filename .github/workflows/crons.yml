name: Strategy Engine Crons

on:
  schedule:
    # Market sync — every 6 hours
    - cron: '0 */6 * * *'
    # Price snapshots — every 5 minutes
    - cron: '*/5 * * * *'
    # Strategy scan + auto-trade — every 5 minutes
    - cron: '1,6,11,16,21,26,31,36,41,46,51,56 * * * *'
    # Trade resolution — every 30 minutes
    - cron: '*/30 * * * *'
    # Portfolio snapshot — every hour
    - cron: '0 * * * *'
    # Strategy tuning — weekly Sunday midnight
    - cron: '0 0 * * 0'
  workflow_dispatch:
    inputs:
      job:
        description: 'Which job to run'
        required: true
        type: choice
        options:
          - sync
          - snapshot
          - scan
          - resolve
          - portfolio
          - tune
          - orderbook
          - all

jobs:
  sync:
    if: github.event.schedule == '0 */6 * * *' || (github.event_name == 'workflow_dispatch' && (inputs.job == 'sync' || inputs.job == 'all'))
    runs-on: ubuntu-latest
    steps:
      - name: Sync markets
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/markets/sync"

  snapshot:
    if: github.event.schedule == '*/5 * * * *' || (github.event_name == 'workflow_dispatch' && (inputs.job == 'snapshot' || inputs.job == 'all'))
    runs-on: ubuntu-latest
    steps:
      - name: Price snapshots
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/prices/snapshot"

  scan:
    if: github.event.schedule == '1,6,11,16,21,26,31,36,41,46,51,56 * * * *' || (github.event_name == 'workflow_dispatch' && (inputs.job == 'scan' || inputs.job == 'all'))
    runs-on: ubuntu-latest
    steps:
      - name: Strategy scan + auto-trade
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/strategies/scan"

  resolve:
    if: github.event.schedule == '*/30 * * * *' || (github.event_name == 'workflow_dispatch' && (inputs.job == 'resolve' || inputs.job == 'all'))
    runs-on: ubuntu-latest
    steps:
      - name: Resolve trades
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/trades/resolve"

  portfolio:
    if: github.event.schedule == '0 * * * *' || (github.event_name == 'workflow_dispatch' && (inputs.job == 'portfolio' || inputs.job == 'all'))
    runs-on: ubuntu-latest
    steps:
      - name: Portfolio snapshot
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/portfolio/snapshot"

  tune:
    if: github.event.schedule == '0 0 * * 0' || (github.event_name == 'workflow_dispatch' && (inputs.job == 'tune' || inputs.job == 'all'))
    runs-on: ubuntu-latest
    steps:
      - name: Tune strategies
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/strategies/tune"

  orderbook:
    if: github.event.schedule == '*/5 * * * *' || (github.event_name == 'workflow_dispatch' && (inputs.job == 'orderbook' || inputs.job == 'all'))
    runs-on: ubuntu-latest
    steps:
      - name: Orderbook snapshots
        run: |
          curl -sf -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/orderbook/snapshot"
